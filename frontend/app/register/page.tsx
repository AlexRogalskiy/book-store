import Register from "@/app/components/Register";

export const metadata = {
    title: "Register",
    description: "Register page",
}


const Page = () => {
    return (
        <Register />
    )
};

export default Page;